.git
.gitignore
.gitattributes
.gitmodules

# Virtualenvs / pip caches
.venv
venv
env
ENV
**/__pycache__
*.py[cod]
*.egg-info

# Local secrets — provided to the container via env_file / -e
.env
.env.*
!.env.example

# IDE / OS junk
.idea
.vscode
.DS_Store
Thumbs.db
*.swp
*.swo

# Build artifacts
build
dist

# Per-request generated outputs (created at runtime)
static/[0-9]*.[0-9]*/

# Logs
*.log

# Docker / CI assets shouldn't ship inside the image
Dockerfile
docker-compose.yml
.dockerignore
.github
README*.md

# Claude / local tooling
.claude
